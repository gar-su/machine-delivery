# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目定位

短剧投放全自动化链路（机器投放系统）的仓库。目标：打通「素材自动生产 × 智能打分 × 机器投放 × 数据回流」全自动投放链路，**2026-09-30 上线 MVP**（影子/小范围跟投 + 执行记录）。

**当前状态**：规划期（M0，2026-08-17~08-28）。**无后端代码**；已有 PRD v1.3（权威设计）、六模块设计 spec（历史归档）、后台原型。本仓库是机器投放系统的目标落地目录。

## 关键文档

- `docs/机器投放系统-功能需求.md` — **权威设计（PRD v1.3）**：系统=触发编排层，接收触发信号、唯一动作是把目标绑定进 Meta / TikTok 默认自动化任务；含 13 条关键决策（§7）。取代了六份 spec 的执行侧设计，实现前先读
- `docs/项目整体规划.md` — 项目全景：现状盘点、里程碑、人事编制、系统对接范围、风险与决策点
- `docs/六模块设计索引.md` — 六模块设计总览：模块职责边界、跨模块关键契约、M0 待对齐清单
- `index.html` — 后台原型（运行看板仅执行记录一页 + 测新清单 + 默认自动化任务）
- `docs/superpowers/specs/2026-08-11-*.md` — 六模块设计 spec（历史归档）：执行侧（序1 执行端 / 序3 账户池 / 序5 看板）已被 PRD 取代；**序2 数据管道契约、序4 系统对接、序6 P1 集成**仍为有效参考
- 机器投放系统最初 spec：`/Users/gar/auto_delivery/docs/superpowers/specs/2026-07-01-machine-delivery-design.md` — PRD 前身，仅在追溯设计演进时参考

## 全链路与现有模块

```
素材生产 → 智能打分 → 机器投放 → 数据回流
```

各环节现有代码在独立仓库，均为离线 demo 级脚本，尚未集成进投放链路：

| 环节 | 仓库 | 状态 |
|---|---|---|
| 素材自动生产 | `/Users/gar/short-drama-automation/`（narration_pipeline 旁白流水线） | 可用 |
| 智能打分 | `/Users/gar/short-drama-scoring/`（时间序列模型） | 可用 |
| 三步流水线投放 | `/Users/gar/auto_delivery/`（查素材→绑定→建任务） | 运行中，每步人工确认 |
| 生命周期引擎 | `/Users/gar/smart_ad_put/` | 已实现，未上线 |
| 需求原型 | `/Users/gar/auto_put_tiktok/` | 活跃迭代 |
| 机器投放系统 | **本仓库** | 设计完成，零代码 |
| 数据管道 / 账户池 | 无 | 完全待建设 |

## 机器投放系统设计要点（PRD v1.3，待实现）

- **定位**：触发编排层——接收触发信号，**唯一动作是把目标分别绑定进 Meta / TikTok 默认自动化任务**。不自建广告、不新建任务、不维护账户池、不跨账户复制（这些能力由既有自动化任务承担）
- **触发信号**：仅两类输入，`signal_id` 为幂等键（同信号重复投递不重复提醒/不重复绑定）。`NEW_DRAMA_ONLINE`（剧上线 → 信号仅提醒、新剧入测新清单 → 用户勾选短剧批量发起测新 → 弹窗确认默认自动化任务配置列表 + 本批统一排期 → 按语种自动绑定测新默认任务并跳转执行记录）；`FOLLOW_UP`（smart_ad_put 决策 → 全自动绑定进跟投任务，无人工）
- **默认自动化任务**：后台维护「场景（测新/跟投）× 语言」默认绑定表（测新按当前操作人逐人维护、跟投归系统）；**本期仅建测新配置**——配置页为已配置条目列表（不铺全量组合）+「新增配置」按钮（场景固定测新、选语种 + 逐平台绑定，已覆盖语种禁重复新增，每行可删除，列表只读、改绑 = 删除后重新新增），跟投配置不建界面（信号到达仍按「系统」版解析）；测新在用户批量发起、弹窗确认时按各短剧语种自动解析默认绑定（弹窗仅展示配置列表，默认自动化任务页列表只读），跟投在信号到达时按语种解析自动绑定。任务直接承载全部执行参数，机器投放不重复定义、不传递参数、无模板中间层
- **编排状态链**：一次编排两环节——绑定（成功/跳过·无默认自动化任务/跳过·数据异常）→ 运行（启动/暂停）；管理看板仅展示「运行」列，绑定跳过/失败并入该列以文本呈现
- **存储**：编排记录（信号接收/绑定结果留痕）；本期无鉴权、内网可访问
- **管理界面**：运行看板仅执行记录一页（纯读）+ 测新清单（分页 10/20/50，支持按语言/短剧名/剧本编号筛选）+ 默认自动化任务（交互；测新确认在测新清单批量发起流程内以批量级弹窗完成——展示默认自动化任务配置列表 + 本批统一排期，无独立清单页），REST API + 前端 SPA（原型见根 `index.html`）
- **解耦**：外部依赖经 Provider 抽象注入（信号通道 / 自动化任务 API）；信号通道实现（文件/API/MQ）未锁，开发期定

## 里程碑

- **M0 立项/招聘/契约冻结**（08-17~08-28）：需求与数据契约冻结、与当前系统对接调研、FB OAuth 权限批量修复
- **M1 并行开发**（08-31~09-18）：P1 集成 / 数据管道+账户池 / 机器投放核心 三线并行
- **M2 联调上线**（09-21~09-30）：影子模式验证 → 09-30 MVP 上线

硬前提（任一不满足则顺延）：8 月底后端/数据到岗、数据契约 8 月底冻结、FB OAuth 修复。

## 待对齐决策（M0 内闭环）

完整待对齐清单按模块分列于 `docs/六模块设计索引.md` §4。要点：

1. 机器投放替换节奏——三步流水线观察期时长、退役决策人归属（P1 集成 spec 序6）
2. 数据管道口径——ROI 口径统一（决策端 vs 打分模型）、30min SLA 边界、素材日级数据入 Hive 装载链路（数据管道 spec 序2）
3. 渠道优先级——Meta / TikTok 并行还是先后验证（系统对接 spec 序4，FB OAuth 修复为前置）
4. 信号通道实现——文件/API/MQ 未锁（PRD §5.4）
5. 规则与阈值归属——打分阈值/跟投规则由运营还是研发维护

## 开发约定

- 全局工程规范见 `/Users/gar/CLAUDE.md`（自动加载，此处不重复）：Python 文件修改后须跑 `mypy --strict` / `ruff` / `pyright` 等强制校验
- 与外部系统（报表/binding/广告平台）对接沿用 `auto_delivery` 的模式：Pydantic models + `ApiClient` 注入鉴权
